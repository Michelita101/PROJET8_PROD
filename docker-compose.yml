version: "3.9"

services:
  api:
    build: ./api
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data

  streamlit:
    build: ./streamlit_app
    ports:
      - "8501:8501"
    depends_on:
      - api
